version: '3'

services:
  relay-you-ip:
    container_name: relay-you-ip
    restart: always
    build:
      context: .
    env_file:
      - .env
    volumes:
      - /etc/ssl/certs/ca-certificates.crt:/etc/ssl/certs/ca-certificates.crt:ro
      - /etc/localtime:/etc/localtime:ro